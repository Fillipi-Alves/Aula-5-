print(): Usado para imprimir texto ou valores no console.
print("Olá, mundo!")

​
input(): Usado para obter a entrada do usuário a partir do teclado.
nome = input("Qual é o seu nome? ")

​
len(): Retorna o comprimento de uma sequência, como uma string, char(texto), lista ou tupla.
texto = "Python"
comprimento = len(texto)  # Isso irá retornar 6

​
type(): Retorna o tipo de um objeto.
x = 5
tipo = type(x)  # Isso irá retornar <class 'int'>

​
int(), float(), str(): Usados para converter valores de um tipo para outro.
numero = int("42")    # Converte uma string para um número inteiro
texto = str(3.14)     # Converte um número de ponto flutuante para uma string

​
list(), tuple(): Usados para converter uma sequência (como uma string) em uma lista ou tupla.
texto = "Python"
lista = list(texto)   # Converte a string em uma lista ['P', 'y', 't', 'h', 'o', 'n']

​
range(): Cria uma sequência de números inteiros em um intervalo especificado.
numeros = range(1, 6)  # Cria uma sequência de 1 a 5

​
sum(): Calcula a soma dos elementos em uma sequência, como uma lista, conjunto e tuplas.  
numeros = [1, 2, 3, 4, 5]
soma = sum(numeros)  # Isso irá retornar 15

​
min(), max(): Encontra o valor mínimo e máximo em uma sequência (listas, tuplas e conjuntos).
numeros = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]
minimo = min(numeros)  # Isso irá retornar 1
maximo = max(numeros)  # Isso irá retornar 9

​
sort(): Retorna uma nova lista ordenada a partir dos elementos de uma sequência.
numeros = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]
numeros_ordenados = sort(numeros)  # Isso irá retornar [1, 1, 2, 3, 3, 4, 5, 5, 5, 6, 9]
